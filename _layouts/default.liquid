---
nav:
  - link: /settings
    label: Settings
  - link: /sequences
    label: Sequences
---
<!DOCTYPE html>
<html lang="en">
  {% include 'head', site: site, page: page, title: title %}
  <body class='bg-light text-dark font-body max-w-screen-md m-auto px-1 lg:px-3'>
    <div data-firebase></div>
    <div data-firebase-auth-check {% if page.url == "/" %}class="hidden"{% endif %}></div>
    {% include 'ie-notice'%}
    {% unless page.url == "/"%}
      {% include 'header', title: site.title, nav: nav %}
    {% endunless %}
    <main class="m-auto min-h-screen" id="main">
      {{content}}
    </main>
  </body>
</html>
