<!DOCTYPE html>
<html lang="en">
  {% render 'head', site: site, page: page, title: title %}
  <body>
    {% render 'ie-notice'%}
    <header>header</header>
    <main id="main">
      {{content}}
    </main>
  </body>
</html>
